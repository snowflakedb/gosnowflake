package gosnowflake

/*
func TestPostAuthError(t *testing.T) {
	sr := &snowflakeRestful{
		Host: "testhost",
		Port: 443,
		LoginTimeout: 1,
	}

	jsonBody := []byte(`{"CODE":123}`)

	params := &url.Values{}
	_, err := sr.PostAuth(params, headers, jsonBody, sr.LoginTimeout)
	if err == null {
		t.Errorf("Failed to get error")
	}
}
*/
